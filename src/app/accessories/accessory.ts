export class Accessory {
    constructor(public id: number, public name: string, public description: string, public price: number, public imagePath: string[], public categoryId: string, public subCategoryId: string) {}
}